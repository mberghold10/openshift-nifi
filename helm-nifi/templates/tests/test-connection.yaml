apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-nifi.fullname" . }}-test-connection"
  labels:
    {{- include "helm-nifi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-nifi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
