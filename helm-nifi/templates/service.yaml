apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-nifi.fullname" . }}
  labels:
    {{- include "helm-nifi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-nifi.selectorLabels" . | nindent 4 }}
